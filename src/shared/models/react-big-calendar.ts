export enum NavigationAction {
  TODAY = 'TODAY',
  PREV = 'PREV',
  NEXT = 'NEXT'
}
export enum ViewOption {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
  AGENDA = 'agenda'
}

export enum FetchType {
  USER = 'USER',
  SERVICE = 'SERVICE',
}

export enum CalendarType {
  STAFF = 'STAFF',
  CLIENT = 'CLIENT',
  ADMIN = 'ADMIN',
  BOOK = 'BOOK'
}